import { RevealTrackerWorkbench } from "@/components/reveal-tracker-workbench";

export const dynamic = "force-dynamic";

export default function RevealTrackerPage() {
  return <RevealTrackerWorkbench />;
}
